# SwiftUI SDK 27 Router

Use this reference when a task mentions SDK or OS 27, or one of the APIs and diagnostics below. Read only the matching focused reference.

| Trigger | Reference |
|---|---|
| `@State` initialization error, synthesized-property redeclaration, missing memberwise initializer | `references/sdk-27-state.md` |
| `@ContentBuilder`, ambiguous `overlay`/`background`, `TupleContent`, empty builder, Charts type-check timeout | `references/sdk-27-content-builder.md` |
| `reorderable`, `reorderContainer`, `ReorderDifference`, swipe actions outside `List` | `references/sdk-27-collection-interactions.md` |
| `AsyncImage` caching, `URLRequest`, or custom `URLSession` | `references/sdk-27-async-image.md` |
| Item-binding `alert` or `confirmationDialog` | `references/sdk-27-presentations.md` |
| Toolbar overflow, visibility priority, pinned items, minimization, or dynamic toolbar content | `references/sdk-27-toolbar.md` |
| Soft-deprecated API migration | `references/latest-apis.md` and `references/soft-deprecation.md` |

Gate runtime APIs when the deployment target is older than their availability. Some SDK 27 conformances back-deploy; each focused reference calls those out explicitly.
